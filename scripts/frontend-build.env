# 前端（默认主题）的来源与构建参数
#
# 源码就在本仓库：frontend/（**快照导入**，不带上游历史）
#   上游仓库：https://github.com/komari-monitor/komari-web
#   导入 commit：4a74e8a81e2e4b1c3da8ad795f9523151efb6b56（tag 1.4.3）
# 导入时把我们此前的 6 个补丁**内联进了源码**：
#   更新检查指向本仓库 / 构建时间可用 SOURCE_DATE_EPOCH 覆盖 / 删除插件系统 /
#   去掉非 HTTPS 告警横幅 / 删除通知系统 / 安装命令与镜像指向本仓库
# 因此本仓库里既没有 patches/ 目录，构建期也不会去克隆上游。

# 后台“发现新版本”检查指向的自有仓库（owner/repo），构建期传给 vite。
KOMARI_UPDATE_REPO="zhemed/komari"

# 构建时间：上游导入 commit 的提交时间（1786612131），保证产物可复现。
KOMARI_FRONTEND_SOURCE_DATE_EPOCH="1786612131"

# web/public/defaultTheme 目录树的规范化哈希
# （tar --sort=name --mtime=@0 --owner=0 --group=0 --numeric-owner | gzip -n -9 | sha256）。
# 由 scripts/build-frontend.sh 生成后回填；非空时脚本强制校验，不一致即失败，
# 用于区分“能构建”与“可复现”。
# 2026-09-16 变更记录：补丁 0006（安装命令/镜像/GitHub 链接改指 zhemed/komari）后为 af0bd793…；
# 源码 vendor 化（导入 frontend/）后重建仍为该值，证明导入没有改变产物。
# 2026-09-17 变更记录（af0bd793… → 25e318e2…）：面板一键升级功能的前端部分——
#   AdminPanelBar 的"有新版本"弹窗加"立即升级/安装此版本/复制 pull 命令"与进度展示、
#   系统设置页加"服务器升级"开关与来源仓库输入、5 个语言各新增 upgrade 段 22 个键。
# 2026-09-17 二次变更（25e318e2… → abd793b3…）：容器/无 systemd 形态下也给出"复制升级命令"
#   入口（0.0.9 的缺口：不支持一键升级时把取命令的按钮一起藏了），5 语言加 copy_pull_command。
# 2026-09-17 三次变更（abd793b3… → c9bd51b3…）：容器一键升级（重建容器）的界面——
# mode 感知的按钮文案（立即升级（重建容器）/复制升级命令）、Docker socket 权限提示、
# 状态未知时不渲染按钮（修 0.0.9/0.0.10 记的观感问题）、5 语言补 2 键。
# 2026-09-17 四次变更（c9bd51b3… → c92ceb77…）：容器零配置网页升级的界面——
# "立即升级（容器内替换）"文案、原地重启阶段、重建容器会回退的提示（5 语言各 3 键）。
FRONTEND_TREE_SHA256="c92ceb77601a285d69eab8474c02e7a644a3a263365fdde78c3012f0bb74bc7f"
