# agent 的来源与构建参数
#
# 源码就在本仓库：agent/（**快照导入**，不带上游历史）
#   上游仓库：https://github.com/komari-monitor/komari-agent
#   导入 commit：1186aafb0d41445daac05d8d282d748a897fd495（2026-08-07，**1.4.3 同期**）
# 导入时把我们此前的改动**内联进了源码**（自更新目标改指本仓库、加资产过滤、容器内跳过、
# 默认关自动更新；安装脚本的下载源/默认目录/默认版本/.exe 修正），因此本仓库里
# 既没有 patches-agent/ 目录，构建期也不会去克隆上游。
#
# 为什么停在 1.4.3 同期而不是上游最新 agent：见 docs/MAINTAINING.md §11.5。
# （0.0.4 曾误用上游 agent 1.5.10，带进 motd 安全告警注入等 1.5 行为。）

# 自更新目标仓库：构建期用 -X 注入，源码里也有同值默认（agent/update/update.go）。
KOMARI_AGENT_UPDATE_REPO="zhemed/komari"

# 发布时使用的 Go 工具链（纯 Go、CGO_ENABLED=0，不需要 gcc/zig）。不一致时只告警。
KOMARI_AGENT_GO_VERSION="go1.26.6"
