diff --git a/cmd/flags/flag.go b/cmd/flags/flag.go
index 272af88..a023000 100644
--- a/cmd/flags/flag.go
+++ b/cmd/flags/flag.go
@@ -2,7 +2,8 @@ package flags_pkg
 
 type Config struct {
 	AutoDiscoveryKey    string  `json:"auto_discovery_key" env:"AGENT_AUTO_DISCOVERY_KEY"`         // 自动发现密钥
-	DisableAutoUpdate   bool    `json:"disable_auto_update" env:"AGENT_DISABLE_AUTO_UPDATE"`       // 禁用自动更新
+	EnableAutoUpdate    bool    `json:"enable_auto_update" env:"AGENT_ENABLE_AUTO_UPDATE"`         // 启用自动更新（本 fork 默认关闭）
+	DisableAutoUpdate   bool    `json:"disable_auto_update" env:"AGENT_DISABLE_AUTO_UPDATE"`       // [deprecated] 自动更新默认已关闭，仅为兼容旧配置保留
 	DisableWebSsh       bool    `json:"disable_web_ssh" env:"AGENT_DISABLE_WEB_SSH"`               // 禁用远程控制（web ssh 和 rce）
 	MemoryModeAvailable bool    `json:"memory_mode_available" env:"AGENT_MEMORY_MODE_AVAILABLE"`   // [deprecated] 已弃用，请使用 MemoryIncludeCache
 	Token               string  `json:"token" env:"AGENT_TOKEN"`                                   // Token
diff --git a/cmd/root.go b/cmd/root.go
index 6c9d34a..3b15cd5 100644
--- a/cmd/root.go
+++ b/cmd/root.go
@@ -117,8 +117,11 @@ var RootCmd = &cobra.Command{
 		if flags.IgnoreUnsafeCert {
 			http.DefaultTransport.(*http.Transport).TLSClientConfig = &tls.Config{InsecureSkipVerify: true}
 		}
-		// 自动更新
-		if !flags.DisableAutoUpdate {
+		if flags.DisableAutoUpdate {
+			log.Println("WARNING: --disable-auto-update is deprecated and has no effect; automatic updates are disabled by default.")
+		}
+		// 自动更新默认关闭，需要显式 --enable-auto-update（或 AGENT_ENABLE_AUTO_UPDATE=1）开启
+		if flags.EnableAutoUpdate {
 			err := update.CheckAndUpdate()
 			if err != nil {
 				log.Println("[ERROR]", err)
@@ -136,7 +139,9 @@ var RootCmd = &cobra.Command{
 func Execute() {
 	for i, arg := range os.Args {
 		if arg == "-autoUpdate" || arg == "--autoUpdate" {
-			log.Println("WARNING: The -autoUpdate flag is deprecated in version 0.0.9 and later. Use --disable-auto-update to configure auto-update behavior.")
+			log.Println("WARNING: The -autoUpdate flag is deprecated. Use --enable-auto-update to enable automatic updates.")
+			// 本 fork 默认关闭自动更新，旧的 -autoUpdate 语义仍是“开启”，照旧生效
+			flags.EnableAutoUpdate = true
 			// 从参数列表中移除该参数，防止cobra解析错误
 			os.Args = append(os.Args[:i], os.Args[i+1:]...)
 			break
@@ -160,7 +165,8 @@ func init() {
 	RootCmd.PersistentFlags().StringVarP(&flags.Endpoint, "endpoint", "e", "", "API endpoint")
 	//RootCmd.MarkPersistentFlagRequired("endpoint")
 	RootCmd.PersistentFlags().StringVar(&flags.AutoDiscoveryKey, "auto-discovery", "", "Auto discovery key for the agent")
-	RootCmd.PersistentFlags().BoolVar(&flags.DisableAutoUpdate, "disable-auto-update", false, "Disable automatic updates")
+	RootCmd.PersistentFlags().BoolVar(&flags.EnableAutoUpdate, "enable-auto-update", false, "Enable automatic updates (disabled by default)")
+	RootCmd.PersistentFlags().BoolVar(&flags.DisableAutoUpdate, "disable-auto-update", false, "[deprecated] Automatic updates are disabled by default")
 	RootCmd.PersistentFlags().BoolVar(&flags.DisableWebSsh, "disable-web-ssh", false, "Disable remote control(web ssh and rce)")
 	//RootCmd.PersistentFlags().BoolVar(&flags.MemoryModeAvailable, "memory-mode-available", false, "[deprecated]Report memory as available instead of used.")
 	RootCmd.PersistentFlags().Float64VarP(&flags.Interval, "interval", "i", 3.0, "Interval in seconds")
diff --git a/update/update.go b/update/update.go
index efd0495..5f0759c 100644
--- a/update/update.go
+++ b/update/update.go
@@ -20,7 +20,10 @@ import (
 
 var (
 	CurrentVersion string = "0.0.1"
-	Repo           string = "komari-monitor/komari-agent"
+	// 自更新目标仓库：本 fork 固定指向自有仓库 zhemed/komari，agent 资产与服务器资产
+	// 在同一个 release 里发布（详见 docs/MAINTAINING.md 的 agent 发行线一节）。
+	// scripts/build-agent.sh 仍会用 -X 注入一次，双保险。
+	Repo string = "zhemed/komari"
 )
 
 const (
@@ -247,6 +250,12 @@ func DoUpdateWorks() {
 }
 
 func checkAndUpdateStable(currentSemVer semver.Version, updater *selfupdate.Updater) error {
+	// 容器里替换二进制会随容器重建丢失，改为提示更新镜像（与 snapshot 通道一致）。
+	if isContainerAgent() {
+		log.Println("Agent is running in a container; skip binary self-update. Pull a newer ghcr.io/zhemed/komari-agent image instead.")
+		return nil
+	}
+
 	latest, err := updater.UpdateSelf(currentSemVer, Repo)
 	if err != nil {
 		return fmt.Errorf("failed to check for updates: %v", err)
@@ -275,7 +284,7 @@ func checkAndUpdateStable(currentSemVer semver.Version, updater *selfupdate.Upda
 
 func checkAndUpdateSnapshot(updater *selfupdate.Updater) error {
 	if isContainerAgent() {
-		log.Println("Snapshot agent is running in a container; skip binary self-update. Refresh the ghcr.io image tagged 'snapshot' instead.")
+		log.Println("Agent is running in a container; skip binary self-update. Pull a newer ghcr.io/zhemed/komari-agent image instead.")
 		return nil
 	}
 
@@ -321,7 +330,13 @@ func CheckAndUpdate() error {
 	log.Println("Checking update...")
 
 	http.DefaultClient = dnsresolver.GetHTTPClient(60 * time.Second)
-	updater, err := selfupdate.NewUpdater(selfupdate.Config{})
+	// 必须限定资产前缀：同一个 release 里既有服务器的 komari-linux-amd64，也有 agent 的
+	// komari-agent-linux-amd64，而 go-github-selfupdate 按“后缀”匹配资产
+	// （selfupdate/detect.go 的 findAssetFromRelease 用 strings.HasSuffix），
+	// 不过滤就可能把自己刷成服务器二进制。
+	updater, err := selfupdate.NewUpdater(selfupdate.Config{
+		Filters: []string{"^komari-agent-"},
+	})
 	if err != nil {
 		return fmt.Errorf("failed to create updater: %v", err)
 	}
