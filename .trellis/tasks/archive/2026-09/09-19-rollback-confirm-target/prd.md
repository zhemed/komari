# 回滚：先确认目标，再执行

## 背景（我的失误）

用户从 "取法构建方式，回滚之前的方式" 起，连续多次要求"回滚"。我**没有先建任务、没有先确认对象**，
直接按猜测动手，且改了三次方向（README 源码构建段 → Dockerfile digest → compose 文件名），
用户评价"瞎干、一直错误方向"。本轮改为任务驱动：先建任务、写清目标选项，确认后一次做完。

## 当前实际状态（截至 2026-09-19，供对照）

| 对象 | 现在是什么状态 | 谁改的 |
|---|---|---|
| Dockerfile / Dockerfile.agent 基础镜像 | `FROM alpine:3.21`（tag） | 我 2026-09-19 回滚（原为 digest） |
| compose 文件名 | 仓库脚本/文档/测试 = `docker-compose.yml`；生产 `/opt/docker/komari/` = `docker-compose.yml`（容器标签已跟随） | 我 2026-09-19 回滚（曾改 compose.yaml） |
| README | 71 行，含「源码构建」段落 | 我先删减（53 行）后又加回该段 |
| 生产部署形态 | docker compose（host 网络 + 挂 docker.sock），版本 0.0.18 | 我 2026-09-18 从 systemd 迁过去 |
| 升级方式 | 挂 socket → 拉镜像 + helper 重建容器 | 我 2026-09-18 起生效 |
| 0.0.18 里的功能改动 | compose tag 自动同步、host 网络下自身容器识别修复 | 我 2026-09-19 发布 |
| 仓库构建产物 | `dist/` 只剩两份 SHA256SUMS（二进制在体检时删了）；`.build` 缓存被清（保留 zig 工具链） | 我 2026-09-18 体检 |

## 候选回滚目标（请点一个，或直接写 X → Y）

- **A** 生产部署回 **systemd**：停 compose → 数据同步回 `/opt/komari/data` → `systemctl enable --now komari`
  （注意那边的二进制是 0.0.14，需同时用 `install-komari.sh` 装 0.0.18，否则新旧版本交叉读库）
- **B** 升级方式回 **"容器内替换"**：去掉 socket 挂载（compose 文件 + 安装脚本默认值一起改），
  面板"重建容器"模式随之停用
- **C** 版本回 **0.0.17**：把生产镜像/二进制退回上一个发布（GitHub release 与镜像都在）
- **D** 仓库某项具体改动的回滚：请指明文件或行为（例如"把 XX 改回 YY"）
- **E** 构建产物/缓存恢复：把体检时删掉的 `dist/` 二进制与 `.build` 缓存重新构建出来

## 约束

- 未确认前**不改任何代码、不碰生产**；
- 确认后一次做完并给验证证据（含生产健康与数据完好）；
- 走完流程：PRD → 执行 → check-repo → journal → 归档。

## 验收

- [ ] 用户点明目标（一个字母或一句 X → Y）
- [ ] 按目标执行并验证
- [ ] journal + 归档
