# compose 文件名核实与切换（compose.yaml vs docker-compose.yml）

## Goal

用户质疑一个说法：compose.yaml 是 Compose v2 官方首选名、docker-compose.yml 是 v1 历史名。要求核实真假（判别性实验），若属实则把 install-compose.sh 生成的文件名、生产部署与文档统一切到 compose.yaml。

## Requirements

- TBD

## Acceptance Criteria

- [ ] TBD

## Notes

- Keep `prd.md` focused on requirements, constraints, and acceptance criteria.
- Lightweight tasks can remain PRD-only.
- For complex tasks, add `design.md` for technical design and `implement.md` for execution planning before `task.py start`.
