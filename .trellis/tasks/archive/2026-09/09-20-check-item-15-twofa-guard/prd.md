# 自检第 15 项：2FA 不得被重新引入

## 背景

0.0.19 已按用户要求**彻底移除 2FA**（两层：账号级登录验证 + 敏感操作二次验证）。
用户选择加一条机械护栏，防止以后被悄悄加回来。真实回归路径：从上游 1.4.3 快照拷代码时带上
`pquerna/otp`、或看到数据库还剩 `two_factor` 列就把模型字段补回去。

## 实现

`scripts/check-repo.sh` 新增第 15 项：

- 判据（扫 `*.go` / `go.mod` / `frontend/src`）：`pquerna/otp`、`two_factor`、`TwoFactor`、
  `RequireSensitive2FA`、`api/admin/2fa`
- 命中 → 报红并列出文件，提示"用户 2026-09-20 明确要求彻底移除，不要补回来"
- **判别性验证**：对含 2FA 的历史提交（`0.0.18`）必须报红——证明规则不是永远为绿

## 验收（已实测）

| 测试 | 结果 |
|---|---|
| 当前代码 | 通过（0 命中） |
| 判别性：对历史提交 0.0.18 | 报红 ✅ |
| 手测：往 router.go 塞一行 `api/admin/2fa` 探针 | 第 15 项报红并指出该文件 ✅ |
| 还原探针后 | 全部通过 ✅ |
| `check-repo.sh --full` | 15 项全绿 |
